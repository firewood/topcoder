#include <iostream>
#include <algorithm>
#include <sstream>
#include <cmath>

using namespace std;

int main(int argc, char *argv[])
{
	cout.precision(10);
	int N;
	cin >> N;
	long double ans = N * 3.5;
	cout << ans << endl;
	return 0;
}
