// A.

#include <iostream>
#include <sstream>

using namespace std;

int main(int argc, char *argv[])
{
	int a, b, h;
	cin >> a >> b >> h;
	int ans = ((a + b) * h) / 2;
	cout << ans << endl;
	return 0;
}
