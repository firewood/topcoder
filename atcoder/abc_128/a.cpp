// A.

#include <iostream>
#include <sstream>
#include <algorithm>

using namespace std;

int main(int argc, char *argv[]) {
	int a, p, ans;
	cin >> a >> p;
	ans = ((a * 3) + p) / 2;
	cout << ans << endl;
	return 0;
}
