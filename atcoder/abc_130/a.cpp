// A.

#include <iostream>
#include <sstream>
#include <algorithm>

using namespace std;

int main(int argc, char *argv[]) {
	int x, a;
	cin >> x >> a;
	cout << (x < a ? 0 : 10) << endl;
	return 0;
}
