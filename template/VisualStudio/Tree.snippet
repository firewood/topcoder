<?xml version="1.0" encoding="utf-8"?>
<CodeSnippets  xmlns="http://schemas.microsoft.com/VisualStudio/2005/CodeSnippet">
  <CodeSnippet Format="1.0.0">
    <Header>
      <Title>Tree</Title>
      <Shortcut>Tree</Shortcut>
      <Description>Tree</Description>
      <Author>firewood</Author>
    </Header>
    <Snippet>
      <Code Language="cpp">
        <![CDATA[struct Tree {
	int _size;
	vector<vector<int>> _edges;
	vector<int> _vis;

	Tree(int size) : _size(size), _edges(size), _vis(size) { }

	void build_edges(const vector<int>& a, const vector<int>& b) {
		for (int i = 0; i < a.size(); ++i) {
			_edges[a[i]].emplace_back(b[i]);
			_edges[b[i]].emplace_back(a[i]);
		}
	}

	void dfs(int n) {
		_vis[n] += 1;

		$end$

		for (auto next : _edges[n]) {
			if (_vis[next] <= 0) {
				dfs(next);
			}
		}
	}
};
]]>
      </Code>
    </Snippet>
  </CodeSnippet>
</CodeSnippets>
